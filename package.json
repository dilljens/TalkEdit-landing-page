{
  "name": "talkedit-landing-page",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build:css": "npx tailwindcss -i src/style.css -o assets/style.css",
    "deploy": "npm run build:css && npx wrangler pages deploy . --project-name talk-edit-landing-page --branch main"
  },
  "dependencies": {
    "@noble/ed25519": "^2.2.0"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.1",
    "tailwindcss": "^4.3.1",
    "wrangler": "^4.0.0"
  }
}
